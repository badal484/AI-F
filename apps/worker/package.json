{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aif/db": "*",
    "@aif/shared": "*",
    "bullmq": "^6.1.2",
    "ioredis": "^6.0.0"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "tsx": "^4.23.12"
  }
}
